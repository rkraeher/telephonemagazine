import styled from 'styled-components';
import { NavItem as NavItemComponent } from "react-materialize";

export const Home = styled.a`
    // only need margin for large view, not mobile
    margin-left: 10px;
`;

export const NavItem = styled(NavItemComponent)`
    
    
`;
